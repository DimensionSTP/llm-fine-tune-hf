_target_: src.utils.RewardManager
rewards:
  - _target_: src.utils.ThinkFormatReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.think_format}
    is_enable_thinking: ${is_enable_thinking}

  - _target_: src.utils.AnswerFormatReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.answer_format}

  - _target_: src.utils.MatchReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.match}
    incorrect_penalty: ${reward.match.incorrect_penalty}

  - _target_: src.utils.CodeExecutionReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.code_execution}
    timeout: ${reward.code_execution.timeout}
    wrong_output_penalty: ${reward.code_execution.wrong_output_penalty}
    non_executable_penalty: ${reward.code_execution.non_executable_penalty}

  - _target_: src.utils.RougeReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.rouge}
    rouge_type: ${reward.rouge.type}

  - _target_: src.utils.EquationReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.equation}
    equation_target_column_name: ${reward.equation.target_column_name}
    equation_numbers_column_name: ${reward.equation.numbers_column_name}

  - _target_: src.utils.RetrievalHitReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.retrieval_hit}
    database: ${reward_database}
    embedding: ${reward_embedding}
    retrieval_top_k: ${reward.retrieval.hit.top_k}
    shaping_weight: ${reward.retrieval.hit.shaping_weight}
    rank_margin: ${reward.retrieval.hit.rank_margin}
    stages: ${reward.retrieval.hit.stages}

  - _target_: src.utils.RetrievalnDCGReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.retrieval_ndcg}
    database: ${reward_database}
    embedding: ${reward_embedding}
    retrieval_top_k: ${reward.retrieval.ndcg.top_k}
    reward_mode: ${reward.retrieval.ndcg.reward_mode}
    ndcg_top_ks: ${reward.retrieval.ndcg.top_ks}
    alpha: ${reward.retrieval.ndcg.alpha}
    weighting_mode: ${reward.retrieval.ndcg.weighting_mode}
    epsilon: ${reward.retrieval.ndcg.epsilon}

  - _target_: src.utils.SingleKVReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.single_kv}
    json_parse_weight: ${reward.single_kv.json_parse_weight}

  - _target_: src.utils.MultiKVReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.multi_kv}
    json_parse_weight: ${reward.multi_kv.json_parse_weight}

  - _target_: src.utils.GroundingBBoxReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.grounding_bbox}
    category_token: ${reward.grounding_bbox.category_token}
    format_reward: ${reward.grounding_bbox.format_reward}
    schema_reward: ${reward.grounding_bbox.schema_reward}
    page_reward: ${reward.grounding_bbox.page_reward}
    iou_weight: ${reward.grounding_bbox.iou_weight}
    iou_05_threshold: ${reward.grounding_bbox.iou_05_threshold}
    iou_05_bonus: ${reward.grounding_bbox.iou_05_bonus}
    iou_07_threshold: ${reward.grounding_bbox.iou_07_threshold}
    iou_07_bonus: ${reward.grounding_bbox.iou_07_bonus}
    center_in_gt_bonus: ${reward.grounding_bbox.center_in_gt_bonus}
    large_box_area_threshold: ${reward.grounding_bbox.large_box_area_threshold}
    large_box_penalty: ${reward.grounding_bbox.large_box_penalty}
    hard_negative_iou_threshold: ${reward.grounding_bbox.hard_negative_iou_threshold}
    hard_negative_overlap_penalty: ${reward.grounding_bbox.hard_negative_overlap_penalty}
    positive_duplicate_iou_threshold: ${reward.grounding_bbox.positive_duplicate_iou_threshold}
    min_reward: ${reward.grounding_bbox.min_reward}
    max_reward: ${reward.grounding_bbox.max_reward}
    schema_keys: ${reward.grounding_bbox.schema_keys}
    status_values: ${reward.grounding_bbox.status_values}

  - _target_: src.utils.GroundingSelectionReward
    is_answer_tag: ${reward.is_answer_tag}
    think_start_token: ${reward.think_start_token}
    think_end_token: ${reward.think_end_token}
    answer_start_token: ${reward.answer_start_token}
    answer_end_token: ${reward.answer_end_token}
    eos_token: ${reward.eos_token}
    extraction_profile: ${reward.extraction_profile}
    weight: ${reward.weight.grounding_selection}
    category_token: ${reward.grounding_selection.category_token}
    format_reward: ${reward.grounding_selection.format_reward}
    schema_reward: ${reward.grounding_selection.schema_reward}
    exact_match_reward: ${reward.grounding_selection.exact_match_reward}
    partial_match_weight: ${reward.grounding_selection.partial_match_weight}
    over_selection_penalty: ${reward.grounding_selection.over_selection_penalty}
    wrong_selection_penalty: ${reward.grounding_selection.wrong_selection_penalty}
    min_reward: ${reward.grounding_selection.min_reward}
    max_reward: ${reward.grounding_selection.max_reward}
    schema_keys: ${reward.grounding_selection.schema_keys}